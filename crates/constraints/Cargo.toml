[package]
name = "quent-constraints"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
quent-schema = { path = "../schema", features = ["visitor"] }

[dev-dependencies]
quent-schema = { path = "../schema", features = ["test-utils"] }
