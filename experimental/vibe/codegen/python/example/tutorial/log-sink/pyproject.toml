[build-system]
requires = ["maturin>=1.10,<2"]
build-backend = "maturin"

[project]
name = "quent-tutorial-log-sink"
version = "0.0.0"
requires-python = ">=3.11"

[tool.maturin]
manifest-path = "bridge/Cargo.toml"
module-name = "quent_tutorial_log_sink"
include = [
  { path = "quent_tutorial_log_sink/__init__.pyi", from = "out-dir", to = "" },
  { path = "quent_tutorial_log_sink/py.typed", from = "out-dir", to = "" },
]
