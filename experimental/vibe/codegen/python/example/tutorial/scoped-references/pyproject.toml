[build-system]
requires = ["maturin>=1.10,<2"]
build-backend = "maturin"

[project]
name = "quent-tutorial-scoped-references"
version = "0.0.0"
requires-python = ">=3.11"

[tool.maturin]
manifest-path = "bridge/Cargo.toml"
module-name = "quent_tutorial_scoped_references"
include = [
  { path = "quent_tutorial_scoped_references/__init__.pyi", from = "out-dir", to = "" },
  { path = "quent_tutorial_scoped_references/py.typed", from = "out-dir", to = "" },
]
