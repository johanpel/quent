[build-system]
requires = ["maturin>=1.10,<2"]
build-backend = "maturin"

[project]
name = "quent-tutorial-event-data"
version = "0.0.0"
requires-python = ">=3.11"

[tool.maturin]
manifest-path = "bridge/Cargo.toml"
module-name = "quent_tutorial_event_data"
include = [
  { path = "quent_tutorial_event_data/__init__.pyi", from = "out-dir", to = "" },
  { path = "quent_tutorial_event_data/py.typed", from = "out-dir", to = "" },
]
