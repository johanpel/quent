[build-system]
requires = ["maturin>=1.10,<2"]
build-backend = "maturin"

[project]
name = "quent-tutorial-unit-resource"
version = "0.0.0"
requires-python = ">=3.11"

[tool.maturin]
manifest-path = "bridge/Cargo.toml"
module-name = "quent_tutorial_unit_resource"
include = [
  { path = "quent_tutorial_unit_resource/__init__.pyi", from = "out-dir", to = "" },
  { path = "quent_tutorial_unit_resource/py.typed", from = "out-dir", to = "" },
]
