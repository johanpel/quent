[build-system]
requires = ["maturin>=1.10,<2"]
build-backend = "maturin"

[project]
name = "quent-tutorial-bounded-resource"
version = "0.0.0"
requires-python = ">=3.11"

[tool.maturin]
manifest-path = "bridge/Cargo.toml"
module-name = "quent_tutorial_bounded_resource"
include = [
  { path = "quent_tutorial_bounded_resource/__init__.pyi", from = "out-dir", to = "" },
  { path = "quent_tutorial_bounded_resource/py.typed", from = "out-dir", to = "" },
]
